{% extends 'base.html.twig' %}

{% block title %}Edit Order{% endblock %}

{% block body %}
    <h1>Edit Order</h1>

    {{ include('order/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="mt-3 d-flex gap-2">
        <a href="{{ path('app_order_index') }}" class="btn btn-sm btn-secondary">Back to list</a>

        {{ include('order/_delete_form.html.twig', {'order': order}) }}
    </div>
{% endblock %}
