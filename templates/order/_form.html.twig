{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
        <a href="{{ path('app_order_index') }}" class="btn btn-secondary ms-2">Cancel</a>
    </div>
{{ form_end(form) }}
