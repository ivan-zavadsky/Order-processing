<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta
        name="viewport"
        content="width=device-width,initial-scale=1"
    >
    <title>{% block title %}App{% endblock %}</title>
    <link
        rel="icon"
        href="{{ asset('favicon.svg') }}"
        type="image/svg+xml"
    >
    <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    >

    {% block stylesheets %}
    <style>
        /* Переключаемся на Bootstrap-ориентированную верстку, минимальные правки */
        :root {
            --header-footer-bg: #3b6b88; /* сине-серый цвет */
        }
        html, body { height: 100%; }
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f1f5f9; /* мягкий светлый фон */
            color: #212529;
        }
        .site-header, .site-footer {
            background-color: var(--header-footer-bg);
        }
        .site-header .nav-link,
        .site-header .navbar-brand,
        .site-footer a,
        .site-footer .text {
            color: rgba(255,255,255,0.95);
        }
        .container-main {
            max-width: 1100px;
        }
        /* Небольшая тень и отступ для контента */
        .content-card { border-radius: .5rem; }
    </style>
    {% endblock %}
</head>
<body>
<header class="site-header">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <div
                class="navbar-brand fw-bold"
            >
                Orders processing
            </div>
        </div>
    </nav>
</header>

<main class="flex-grow-1 py-4">
    <div class="container container-main">
        <div class="card content-card shadow-sm p-3 p-md-4 bg-white">
            {% block body %}{% endblock %}
        </div>
    </div>
</main>

<footer class="site-footer mt-auto " style="height: 65px;">
    <div
        class="
            container
            d-flex
            flex-column
            flex-md-row
            justify-content-between
            align-items-center
        "
    >
        <div class="text-muted text-white-50">
        </div>
        <div class="mt-2 mt-md-0">
        </div>
    </div>
</footer>
{% block javascripts %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}

</body>
</html>
