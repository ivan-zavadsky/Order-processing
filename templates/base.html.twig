<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{% block title %}App{% endblock %}</title>
    <link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml">

    <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    >

    {% block stylesheets %}
    <style>
        /* Переключаемся на Bootstrap-ориентированную верстку, минимальные правки */
        :root {
            --header-footer-bg: #3b6b88; /* сине-серый цвет */
        }
        html, body { height: 100%; }
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f1f5f9; /* мягкий светлый фон */
            color: #212529;
        }
        .site-header, .site-footer {
            background-color: var(--header-footer-bg);
        }
        .site-header .nav-link,
        .site-header .navbar-brand,
        .site-footer a,
        .site-footer .text {
            color: rgba(255,255,255,0.95);
        }
        .container-main {
            max-width: 1100px;
        }
        /* Небольшая тень и отступ для контента */
        .content-card { border-radius: .5rem; }
    </style>
    {% endblock %}
</head>
<body>
<header class="site-header">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{{ path('app_product_index') ?: '/' }}">Processing</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNavbar">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_order_index') }}">Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_order_check') }}">
                            Check last order
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product_index') }}">Products</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main class="flex-grow-1 py-4">
    <div class="container container-main">
        <div class="card content-card shadow-sm p-3 p-md-4 bg-white">
            {% block body %}{% endblock %}
        </div>
    </div>
</main>

<footer class="site-footer mt-auto " style="height: 65px;">
    <div
        class="
            container
            d-flex
            flex-column
            flex-md-row
            justify-content-between
            align-items-center
        "
    >
        <div class="text-muted text-white-50">
{#            <strong class="text-white">Processing</strong>#}
{#            <div class="small text-white-50">&copy; {{ "now"|date("Y") }} Все права защищены</div>#}
        </div>
        <div class="mt-2 mt-md-0">
{#            <a class="me-3 text-decoration-none" href="#">Terms</a>#}
{#            <a class="text-decoration-none" href="#">Privacy</a>#}
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
